# Referans dbt projesi (şartname §14 kararı: "dbt = runbook + örnek proje", chart bileşeni DEĞİL).
# Bu dosya ConfigMap olarak kümeye verilir (runbooks/dbt/cronjob.yaml) ya da yerelden `dbt run` ile koşturulur.
name: lakehouse_gold
version: "1.0.0"
config-version: 2
profile: lakehouse          # profiles.yml'daki profil adı

model-paths: [models]
target-path: target
log-path: logs
clean-targets: [target, dbt_packages]

models:
  lakehouse_gold:
    gold:
      # Iceberg tablosu olarak materyalize edilir; hedef şema profiles.yml'daki `schema` (gold).
      +materialized: table
