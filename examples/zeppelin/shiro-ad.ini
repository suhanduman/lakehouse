# Zeppelin Shiro — Active Directory (LDAPS) şablonu. PROD.
#
# Kurulum (glue'dan ÖNCE; components.devSecrets=false olduğu için chart bu Secret'ı üretmez):
#   kubectl -n lakehouse create secret generic zeppelin-shiro --from-file=shiro.ini=shiro-ad.ini
# Değişiklikten sonra: kubectl -n lakehouse rollout restart deploy/zeppelin
#
# DC=/OU= değerlerini, systemUsername/systemPassword'ü ve ldaps ana bilgisayarını müşteri AD'sine göre doldurun.
# LDAPS (636) zorunlu: AD parolaları düz LDAP'ta açık gider. Zeppelin JVM'i AD sunucu sertifikasının
# zincirine güvenmeli (kurumsal kök CA değilse: cacerts'e ekleyin — runbooks/install.md "Kurumsal kök CA").

[main]
activeDirectoryRealm = org.apache.zeppelin.realm.ActiveDirectoryGroupRealm
activeDirectoryRealm.systemUsername = CN=svc-zeppelin,OU=ServiceAccounts,DC=example,DC=com
activeDirectoryRealm.systemPassword = DEĞİŞTİR
activeDirectoryRealm.searchBase = DC=example,DC=com
activeDirectoryRealm.url = ldaps://ad.example.com:636
activeDirectoryRealm.principalSuffix = @example.com
# AD grubu -> Zeppelin rolü (aşağıdaki [urls] rol kapısıyla aynı adlar)
activeDirectoryRealm.groupRolesMap = "CN=lakehouse-admins,OU=Groups,DC=example,DC=com":"admin","CN=lakehouse-analysts,OU=Groups,DC=example,DC=com":"analyst","CN=lakehouse-users,OU=Groups,DC=example,DC=com":"user"
# Grup üyeliği değişiklikleri bir sonraki oturum açmada geçerli olsun (yetki önbelleği yok)
activeDirectoryRealm.authorizationCachingEnabled = false
securityManager.realms = $activeDirectoryRealm

# Rol kapısı: Shiro'nun yerleşik roles[] filtresi TÜM rolleri ister; Zeppelin'in kendi filtresi HERHANGİ birini
# yeterli sayar (imajdan doğrulandı: apache/zeppelin:0.12.1 içinde org/apache/zeppelin/utils/
# AnyOfRolesUserAuthorizationFilter.class; sınıf hasRole(rol) VEYA principal == argüman kontrolü yapar).
# Bu olmadan /** = authc yalnız "kimlik doğrulanmış" der: grup eşlemesi tutmayan HERHANGİ bir AD hesabı
# Zeppelin'e girip paylaşımlı `zeppelin` Trino servis hesabıyla (SELECT tüm katalog, filtre/maske yok) okurdu.
anyofroles = org.apache.zeppelin.utils.AnyOfRolesUserAuthorizationFilter

sessionManager = org.apache.shiro.web.session.mgt.DefaultWebSessionManager
cookie = org.apache.shiro.web.servlet.SimpleCookie
cookie.name = JSESSIONID
cookie.httpOnly = true
cookie.sameSite = LAX
cookie.secure = true                  # Route/Ingress TLS'i sonlandırır: çerez yalnız HTTPS'te
sessionManager.sessionIdCookie = $cookie
securityManager.sessionManager = $sessionManager
securityManager.sessionManager.globalSessionTimeout = 86400000
shiro.loginUrl = /api/login

# [roles] bölümü YOK: roles[]/anyofroles[] filtreleri yalnız rol ÜYELİĞİNE bakar, izin (permission) istemez.
# `admin = *` gibi wildcard izinler burada hiçbir kapıyı açmaz ama ileride isPermitted() kullanan bir kontrol
# eklenirse sessizce her şeyi verir -> footgun; bu yüzden tanımlanmaz.

[urls]
# Sıra önemlidir: İLK EŞLEŞEN kural kazanır.
/api/version = anon
/api/cluster/address = anon
# Giriş/çıkış uç noktalarında rol kapısı OLAMAZ. Shiro'nun authc filtresi loginUrl'de başarısız girişten sonra
# bile zinciri SÜRDÜRÜR (asıl girişi Zeppelin'in LoginRestApi'si yapar) -> rol filtresi orada kimliksiz bir
# subject görür ve AnyOfRolesUserAuthorizationFilter `getPrincipal().equals(rol)` satırında NPE atar:
# canlı sonuç `/api/login` 500 (2026-09-18 ölçüldü). Bu yüzden yalnız authc; veri uç noktaları aşağıda korunur.
/api/login = authc
/api/login/** = authc
/api/interpreter/** = authc, roles[admin]
/api/notebook-repositories/** = authc, roles[admin]
/api/configurations/** = authc, roles[admin]
/api/credential/** = authc, roles[admin]
# SON KURAL: kimlik doğrulanmış OLMAK YETMEZ — groupRolesMap'ten gelen üç rolden en az biri şart.
# Rolü olmayan (grup dışı) kullanıcı buraya düşer ve 401 alır.
/** = authc, anyofroles[admin, analyst, user]
