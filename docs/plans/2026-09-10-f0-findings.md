# F0 Spike Bulguları (2026-09-10 → )

Her satır: ölçülen gerçek (komut çıktısı) → karar. "Çalıştı sanıyorum" yazılmaz; çıktı yapıştırılır.

**Ortam:** macOS arm64, Podman 6.0.2 machine 8 CPU / 11 GB, kind **0.33.0** (0.32 Podman 6 ile uyumsuz: `ps --format` şablon hatası → 0.33 çözdü), kubectl 1.36.3, helm 4.2.3, Python 3.14.

| Spike | Soru | Sonuç (kanıt) | F1/F2 kararı |
|---|---|---|---|
| S0 | kind (podman) + MinIO ayağa kalkıyor mu? | ✅ `kind-lh-spike` oluştu; MinIO rollout + `lakehouse` bucket Job complete (`00-cluster.sh --check` OK) | kind ≥0.33 şart (runbook) |
| S0b | CNPG 1.30: `managed.roles` initdb'den SONRA uygulanır → `postInitApplicationSQL` içindeki `ALTER TABLE … OWNER TO dbz` ilk denemede `role "dbz" does not exist` (initdb job log). Çözüm: rolü aynı SQL listesinin başında `CREATE ROLE dbz LOGIN REPLICATION PASSWORD …` ile yarat; `managed.roles` sonradan sahiplenir. | `runbooks/add-source.md` (Debezium kullanıcısı tablo sahibi olmalı, filtered publication) |
| S0c | Strimzi 1.2 `KafkaConnect` (`kafka.strimzi.io/v1`): `groupId/configStorageTopic/offsetStorageTopic/statusStorageTopic` **spec üst düzeyinde zorunlu** (`config.group.id` vb. kabul edilmez → `spec.groupId: Required value`). | glue chart KafkaConnect şablonu |
| S0d | kind (Podman) altında pod `pids.max=307`: düğüm konteynerinin Podman `pids_limit=2048` × systemd `DefaultTasksMax` %15 → Spark driver `OutOfMemoryError: unable to create native thread` (S4 ilk iki deneme). Çözüm (doğrulandı): `podman update --pids-limit 8192 <node>` (0 kabul edilmiyor) + düğümde systemd drop-in `DefaultTasksMax=infinity` + `daemon-reexec` → yeni pod `pids.max=max`. kubelet `podPidsLimit` pod cgroup'una YANSIMADI (307 kaldı). `00-cluster.sh`'e eklendi. Docker tabanlı CI'da yok. | `test/e2e` kind script'i + runbook "lokal geliştirme" notu |
| S1a | Polaris 1.7 + relational-jdbc (CNPG) + admin-tool bootstrap ayağa kalkıyor mu? | ✅ chart `polaris/polaris 1.7.0` + `persistence.type=relational-jdbc` (CNPG `polaris-db-app` secret: `jdbc-uri/username/password`) + `apache/polaris-admin-tool:1.7.0 bootstrap --realm=POLARIS --credential=POLARIS,root,s3cr3t` Job 62 s'de Complete; deploy Ready. **Dikkat:** health `8182` (management) portunda, API `8181` — readiness `kubectl rollout status` ile beklenmeli. | Polaris chart + bootstrap Job glue'ya aynen |
| S1b | MinIO'ya vended-credentials ile pyiceberg yazma/okuma? | ✅ küme-içi Job (`python:3.13-slim` + pip pyiceberg 0.10): `OK: smoke_vended.t yazıldı/okundu, 2 satır` — MinIO'nun STS AssumeRole'u çalışıyor. **Bulgu:** sunucu LoadTable yanıtında katalogdaki `endpoint`'i (`minio.minio.svc:9000`) döndürür ve istemcinin `s3.endpoint`'ini EZER → laptop'tan port-forward ile yazma DNS'te düşer; istemciler küme içinde çalışmalı (Connect/Spark/Trino zaten öyle). | Runbook: lokal araçlar için `endpoint` dış adres + `endpoint_internal` küme adresi ayrımı |
| S1c | `stsUnavailable=true` + istemci statik anahtarlarıyla yazma/okuma? | ✅ ayrı katalog `lakehouse_nosts` (`catalogs create … --no-sts`; **konumlar başka katalogla ÇAKIŞAMAZ** → ayrı bucket `s3://lakehouse-nosts/`, aksi `ValidationException: locations overlaps`). Üç adım gerekti: (1) Polaris pod'una `AWS_ACCESS_KEY_ID/SECRET` (`extraEnv`) — yoksa `SdkClientException: Unable to load credentials` (chart'ın `storage.secret`→`polaris.storage.aws.*` env'i bu yola yetmiyor); (2) istemci vending İSTEMEMELİ: pyiceberg varsayılan `X-Iceberg-Access-Delegation: vended-credentials` gönderir → `400 Credential vending was requested … but no credentials are available`; header boş veya `none` verilince ✅ `OK: smoke_static.t yazıldı/okundu` (spec: stsUnavailable "effectively disables vending"); (3) istemci `s3.access-key-id/secret-access-key/endpoint/path-style-access`. MinIO'da STS VAR → ana katalog `lakehouse` vended modda çalışıyor (S1b). | **Polaris KALIR.** F1: müşteri S3'ünde STS yoksa katalog `sts_unavailable: true` + Polaris pod'a S3 anahtarı + istemcilerde delegation header kapalı (Connect `iceberg.catalog.header.X-Iceberg-Access-Delegation=none`, Spark `spark.sql.catalog.<c>.header.…=none`, Trino `iceberg.rest-catalog.vended-credentials-enabled=false`) — runbook'a iki mod olarak yazılır |
| S1d | `polaris setup apply setup.yaml` katalog+principal+grant'i tek YAML'dan kuruyor mu? | ✅ `apache-polaris` 1.7.0 CLI: `principal_roles[]`, `principals{name:{type,roles}}`, `catalogs[]{name,type,storage_type,default_base_location,allowed_locations,region,endpoint,endpoint_internal,path_style_access,sts_unavailable,roles{assign_to,privileges{catalog[]}},namespaces[{name}]}` — katalog + 3 namespace + 2 catalog-role + grant'ler + 3 principal tek komutta (`=== Setup Apply Process Completed Successfully ===`). **Dikkat:** principal credential'ları yalnız apply stdout'una JSON olarak basılır (yaratma sırasıyla); root `ROTATE_CREDENTIALS` yetkisine sahip DEĞİL → log kaydedilmeli. Şema `setup export` çıktısıyla aynı. | `runbooks/install.md`: `glue/polaris-setup.yaml` + `polaris setup apply`; credential'ları Secret'a yaz |
| S2a | `spec.build` (Debezium zip + Iceberg maven artefaktları) build süresi / eksik sınıf? | ✅ build+Ready **613 s** (Kaniko, ttl.sh); Strimzi her artefaktı `plugins/iceberg/<hash>/` alt dizinine bağımlılıklarıyla koyar; Debezium RUNNING, topic `shop.public.orders` oluştu. **Eksik sınıf:** `ClassNotFoundException: org.apache.iceberg.aws.s3.S3FileIO` — `iceberg-aws-bundle` yalnız shaded AWS SDK (63 MB, S3FileIO YOK); sınıf `org.apache.iceberg:iceberg-aws`'da → 5. artefakt olarak eklendi. **2. tur:** `NoClassDefFoundError: org/apache/iceberg/orc/OrcRowWriter` — Iceberg 1.11 `iceberg-data` → `GenericFormatModels.register` static init'i Parquet+ORC+Avro format modellerinin hepsini yükler → `iceberg-orc` 6. artefakt (runtime zip'in içeriğiyle birebir örtüşüyor). **3. tur:** `ClassNotFoundException: org.apache.hadoop.conf.Configuration` — parquet-hadoop yazıcısı Hadoop Configuration ister; runtime zip `hadoop-common 3.4.3`'ü (12 exclusion'la) taşır. Biz Spark'ın yolunu seçtik: shaded, transitive'siz `hadoop-client-api` + `hadoop-client-runtime` 3.4.3. Her eksik jar bir yeniden build (~11 dk) = spike'ın bedeli; F1'de liste sabit. | glue `plugins:` bloğu: debezium zip + `iceberg-kafka-connect`, `-transforms`, `iceberg-parquet`, **`iceberg-orc`**, **`iceberg-aws`**, `iceberg-aws-bundle`, **`hadoop-client-api` + `hadoop-client-runtime` 3.4.3** |
| S2b | `KubernetesSecretConfigProvider` ile `${secrets:...}` çözümleniyor mu? | ✅ `config.providers=secrets` + `io.strimzi.kafka.KubernetesSecretConfigProvider` + SA `connect-connect`'e Secret okuma Role'ü; Debezium `database.user/password` ve sink `iceberg.catalog.credential` `${secrets:lakehouse/<secret>:<key>}` ile çözüldü, connector RUNNING. Mount YOK. | glue KafkaConnect: bu yol; v1'in DirectoryConfigProvider mount sözleşmesi gider |
| S2c | Sink `auto-create` namespace'i de yaratıyor mu, yoksa önceden mi? | Namespace'ler `polaris setup apply` (`catalogs[].namespaces[]`) ile deklaratif önceden yaratıldı; sink tabloyu (`shop_raw.orders`) auto-create etti. Sink'in namespace yaratıp yaratmadığı ayrıca test edilmedi (gerekmiyor: setup.yaml zaten kaynak-ekleme runbook'unun parçası). | `runbooks/add-source.md`: kaynak = setup.yaml'a namespace satırı + KafkaConnector CR'ları |
| S2d | `DebeziumTransform`: `_cdc.op` değerleri (I/U/D?), `_cdc.ts` tipi, DELETE satırı `before`'dan mı? | ✅ Bronze şeması: `id long, status string, amount decimal(38,2), updated_at string, _cdc struct<op string, ts timestamptz, offset long, source string, target string, key struct<id long>>`. `_cdc.op ∈ {I,U,D}` (büyük harf; snapshot `r` → I). `amount` Decimal korunuyor (JSON schemas.enable=true + `decimal.handling.mode=precise`), `updated_at` timestamptz → **string** (ISO; v1 R1 bulgusuyla aynı — merge'de `CAST(... AS TIMESTAMP)` gerekir), `_cdc.ts` timestamptz, `_cdc.offset` Kafka offset (dedup tie-break), `_cdc.key` PK struct'ı. **DELETE satırı:** `_cdc.op='D'`, `_cdc.key={id:2}` dolu; iş kolonları `before`'dan gelir ama pg `REPLICA IDENTITY DEFAULT` yalnız PK gönderir → sink `required` kolonları **varsayılanla doldurur** (`status=''`, `amount=0.00`, `updated_at='1970-01-01'`). Merge silme için yalnız key+op kullanır → sorun değil; tam before-image istenirse kaynakta `ALTER TABLE … REPLICA IDENTITY FULL` (DBA runbook seçeneği). Boşta commit'ler snapshot üretmez ('committed to 0 table(s)', rapor 04 ile uyumlu). **Çift yazım:** manuel offset-reset + restart sonrasında 4 snapshot kaydı ikinci kez Bronze'a yazıldı (`rows=10 ops={'I':8,'U':1,'D':1}`) — at-least-once penceresi; Silver merge latest-per-key dedup'ı absorbe eder (Bronze changelog'da kopya kabul edilir). F2'de: sink'in snapshot summary `kafka.connect.offsets.*` kayıtları ve restart davranışı incelenecek (rapor 03: coordinator fencing PR'ları açık). **Operasyonel bulgu:** başarısız task denemelerinden sonra consumer konumu ileri kalmıştı → connector `state: stopped` + `kafka-consumer-groups --reset-offsets --to-earliest` + `running` ile düzeldi (Kafka'nın kendi aracı). **Zorunlu ayar:** `iceberg.kafka.session.timeout.ms=120000`, `heartbeat.interval.ms=15000`, `max.poll.interval.ms=300000`, `request.timeout.ms=130000` — yoksa veri yokken control consumer (`cg-control-*`) 60 s'lik Connect poll'u yüzünden 45 s session'ı kaçırıp sonsuz rebalance'a giriyor → 'committed to 0 table(s)' (sink'in belgeli `iceberg.kafka.*` pass-through'u; v1'de de canlı bulunmuştu). | `merge_cdc.py`: `_cdc.op='D'` silme, `ORDER BY _cdc.ts DESC, _cdc.offset DESC`; glue sink şablonu 4 timeout satırını içerir; runbook: offset-reset adımı |
| S3 | `default-partition-by=day(_cdc.ts)` (nested) kabul edildi mi? | ✅ ikinci sink `shop_part_raw.orders`: `partition spec: [1000: _cdc.ts_day: day(7)]`, `rows=6 ops={'I':4,'U':1,'D':1}` — nested `_cdc.ts` üzerinden gün partition'ı çalışıyor; yeni sink topic'i baştan okuyup tüm CDC'yi (I/U/D) aldı. | spec §5.2 aynen: Bronze `day(_cdc.ts)` partition'lı; TTL metadata-only gün silme |
| S4a | spark-operator 2.5.2 + `apache/spark:4.1.0-java21-python3` + `spark.jars.packages` (Iceberg 1.11) çözümleniyor mu, süre? | ✅ resmi imaj + `spark.jars.packages=iceberg-spark-runtime-4.1_2.13:1.11.0,iceberg-aws-bundle:1.11.0` (`spark.jars.ivy=/tmp/.ivy2`): uçtan uca **181 s** (imaj çekimi + paket çözümü + iş) — kabul edilebilir; özel Spark imajı gerekmez. SA `spark-operator-spark` chart tarafından yaratılır. Polaris'e OAuth2 client-credentials + vended-credentials ile bağlandı. **Bulgu:** 1 GB/1 core driver'da `OutOfMemoryError: unable to create native thread` (200 shuffle partition, metadata sorgusunda) → `spark.sql.shuffle.partitions=8`, memoryOverhead 512m, driver 1536m (tier değerleri). | F2 `ScheduledSparkApplication` şablonu: packages + ivy dizini + tier'a göre shuffle/parallelism |
| S4b | MoR MERGE (`WHEN MATCHED AND op='D' THEN DELETE`) + `rewrite_position_delete_files` + time travel çalıştı mı? ANSI mode CAST etkisi? | ✅ 3. tur 50 s (paketler + iş): `INC` dedup doğru (id1→U, id2→D, id3/4→I); **MERGE Silver** `[1 shipped 11.50, 3 new 3.00, 4 probe 1.00]` (id 2 silindi; Bronze'daki 4 kopya I satırı dedup'ta eridi); `UPDATE` → `POSITION_DELETE_FILES_AFTER_UPDATE 9` (**MoR kanıtı**, Spark position delete yazar); `rewrite_position_delete_files` + `rewrite_data_files(delete-file-threshold=1)` → **0** delete dosyası; `VERSION AS OF <snapshot>` → `new` (**time travel Polaris altında çalışıyor** — kabul testi 264); `ANSI_MODE on` → `CAST('abc' AS INT)` hata (`CAST_INVALID_INPUT`). | `merge_cdc.py`: MoR varsayılan, `_cdc.op='D'`, dedup `ORDER BY _cdc.ts DESC, _cdc.offset DESC`; şema-genişletme/tip dönüşümlerinde açık `CAST`/`try_cast` (Spark 4 ANSI); bakım CR'ları `rewrite_position_delete_files` saatlik |


## F1/F2 için bağlayıcı kararlar (F0 kapanışı, 2026-09-11)

1. **Katalog: Apache Polaris 1.7 KALIR** (S1a–S1d). İki mod runbook'ta: (a) STS'li S3 (MinIO/AWS): vended-credentials; (b) STS'siz S3: katalogda `sts_unavailable: true` + Polaris pod'una `AWS_ACCESS_KEY_ID/SECRET` (`extraEnv`) + istemcilerde delegation header kapalı (`header.X-Iceberg-Access-Delegation=none` / Trino `vended-credentials-enabled=false`) + istemci `s3.*` anahtarları. Katalog konumları çakışamaz → her katalog ayrı bucket/prefix. Kurulum = `polaris/polaris` chart (`persistence.type=relational-jdbc`, CNPG secret) + `polaris-admin-tool bootstrap` Job + `polaris setup apply setup.yaml` (katalog+namespace+rol+grant+principal; credential'lar apply stdout'undan Secret'a; root rotate edemez). Health portu 8182, API 8181.
2. **Connect imajı YOK — `KafkaConnect.spec.build`** (S2a) nihai `plugins:`: Debezium `*-plugin.zip` (Maven Central) + maven: `iceberg-kafka-connect`, `iceberg-kafka-connect-transforms`, `iceberg-parquet`, `iceberg-orc`, `iceberg-aws`, `iceberg-aws-bundle` (1.11.0) + `hadoop-client-api`, `hadoop-client-runtime` (3.4.3). Build ≈ 10–11 dk (Kaniko). Strimzi v1 API: `groupId/configStorageTopic/offsetStorageTopic/statusStorageTopic` spec üst düzeyinde.
3. **Secrets: `io.strimzi.kafka.KubernetesSecretConfigProvider`** (S2b) + Connect SA'ya Secret okuma Role'ü; `${secrets:<ns>/<name>:<key>}`.
4. **Bronze şekli** (S2d): iş kolonları + `_cdc{op∈{I,U,D}, ts timestamptz, offset, source, target, key{pk…}}`; decimal korunur (JSON schemas.enable=true + `decimal.handling.mode=precise`), timestamptz→**string** (merge'de CAST), DELETE satırında iş kolonları varsayılanla dolu (pg REPLICA IDENTITY DEFAULT) — merge yalnız key+op kullanır. Kopyalar olabilir (at-least-once) → dedup zorunlu.
5. **Bronze partition `day(_cdc.ts)`** sink `default-partition-by` ile çalışıyor (S3) → spec §5.2 aynen; TTL gün-hizalı metadata delete.
6. **Sink zorunlu ayarları**: `iceberg.kafka.session.timeout.ms=120000`, `heartbeat.interval.ms=15000`, `max.poll.interval.ms=300000`, `request.timeout.ms=130000` (control consumer flap'ı; veri yokken 'committed to 0 table(s)'); commit aralığı prod'da varsayılan (300 s). Namespace'ler `setup.yaml`'da deklaratif.
7. **Spark: resmi `apache/spark:4.1.0-java21-python3` + `spark.jars.packages`** (S4a) — özel imaj yok; `spark.jars.ivy=/tmp/.ivy2`; küçük tier'da `spark.sql.shuffle.partitions=8`, `spark.default.parallelism=8`, memoryOverhead 512m. **MoR varsayılan** + `rewrite_position_delete_files` saatlik + compaction (S4b). Spark 4 **ANSI mode**: merge/şema-genişletme SQL'inde açık `CAST`/`try_cast`.
8. **Runbook sorun-giderme**: (a) task başarısızlığından sonra consumer konumu → connector `state: stopped` + `kafka-consumer-groups --reset-offsets --to-earliest` + `running`; (b) lokal kind+Podman: kind ≥0.33, düğüm `--pids-limit 8192` + systemd `DefaultTasksMax=infinity` (S0d); (c) CNPG'de Debezium rolü `postInitApplicationSQL` içinde `CREATE ROLE` ile (S0b).
9. **Doğrulama işleri küme içinde koşar** (pyiceberg Job; sunucu küme-içi S3 endpoint'i döndürür) → `test/e2e` deseni bu.

## F1 notu (2026-09-11)
Spike script'leri `glue/`, `platform/`, `test/e2e/`, `runbooks/` olarak ürünleşti; `test/spike/` kaldırıldı (git geçmişinde, son hâli commit 90d1202). F1 e2e'de canlı doğrulananlar / değişenler:

| # | Konu | Canlı bulgu (kind/Podman: helm modu 10m35s, ArgoCD modu 15m05s; GitHub Actions ArgoCD modu: run 34590519941 ✅) | Sonuç |
|---|---|---|---|
| 1 | admin-tool Job | `bootstrap --realm=POLARIS --credential=POLARIS,<id>,<secret>` çalışıyor; CNPG hazır olana kadar 4 pod yeniden başlatması (~2.5 dk, backoffLimit 6). Wave 2'de Connect build'ini bekliyordu → Polaris pod'u bootstrap'a kadar crash-restart | Job **wave 1** (yalnız CNPG'ye bağlı) |
| 2 | Keycloak operator 26.7.3 | Tek tek CRD dosyası listelemek `keycloakoidcclients`/`keycloaksamlclients` CRD'lerini kaçırdı → informer 404 → **CrashLoopBackOff**. Düz `namespace:` ClusterRoleBinding subject'ini `keycloak` ns'inde bıraktı | Uzak kustomize dizini `//kubernetes?ref=26.7.3` (4 CRD) + `NamespaceTransformer(setRoleBindingSubjects: allServiceAccounts)` |
| 3 | Keycloak CR alanları | `k8s.keycloak.org/v2alpha1` (served; storage v2beta1) kabul edildi; `db.usernameSecret/passwordSecret`, `hostname.strict`, `http.httpEnabled`, `proxy.headers`, `bootstrapAdmin` sorunsuz. Koşullar: Keycloak `Ready`, KeycloakRealmImport `Done` | e2e bu iki koşulu bekler |
| 4 | ArgoCD sağlık/wave | Yerleşik sağlık denetimleri Strimzi/CNPG/Keycloak CR'larında çalıştı; glue sync işlemi "waiting for healthy state of KafkaConnect/connect" ile wave 2'yi Connect build'ine kadar tuttu. OCI Helm repo (`quay.io/strimzi-helm`) için `argocd` ns'inde repository Secret şart. Alt Application'lar kökten `targetRevision: v2` ile üretilir → dalın son commit'ini izler | bootstrap.sh Secret'ı uygular; runbook notu |
| 5 | Strimzi CRD OutOfSync | `kafkas.kafka.strimzi.io` şemasında boş `properties: {}` (status.clusterSecurity) API sunucusunca düşürülüyor → kalıcı OutOfSync | `ignoreDifferences` jsonPointer (00-strimzi.yaml) |
| 6 | Connect build süresi | kind (Podman, 8 CPU): 8–9 dk; Strimzi per-reconcile zaman aşımı `StrimziTimeoutException` geçici (sonraki reconcile toparlar). CI (2 vCPU) daha yavaş → Application `Synced` beklemesi 1800 s | run.sh zaman aşımları |
| 7 | Kustomize yükleme kısıtı | Overlay'lerin `../../apps/*.yaml` gevşek dosya referansı `LoadRestrictionsRootOnly` ile hata (ArgoCD repo-server da aynı) | `platform/apps/kustomization.yaml` base + `resources: [../../apps]` |
| 8 | Helm 4 runner'da | `helm plugin install <git>` provenance ister | `--verify=false` (workflow) |
| 9 | S3 kimlik Secret'ı | dev/prod farkı yalnız Secret adıydı | tek ad **`s3-creds`** (dev'de MinIO şablonu üretir); `polaris-dev.yaml` silindi |
| 10 | `polaris setup apply` | stdout `Creating principal: <ad>` + `{"clientId"...}` satırları parser ile uyumlu; 4 Secret yazıldı. Idempotent yeniden çalıştırma canlı ✅ (aynı kümede 2. koşu: mevcut rol/principal/katalog `Skipping creation`, Secret'lar dokunulmadı, log'a credential yazılmadı) | — |
| 11 | Smoke | Küme içi pyiceberg, vended-credentials (MinIO STS): `smoke_vended.t` 2 satır yaz/oku ✅ | — |
| 12 | Prod render yolu | platform: openshift (Route, strict hostname, NP seçicileri) canlı doğrulanmadı — OpenShift pre-ship ortamında | F5 öncesi |
| 13 | Keycloak LDAP bindCredential | realm import düz değer ister; Git'e girmemesi için mekanizma (env-substitution / import-sonrası patch) F4'te kararlaştırılır | F4 |

## F2 notu (2026-09-11)
Plan `docs/plans/2026-09-11-lakehouse-v2-f2-ingestion.md`. Canlı kanıt: kind/Podman (ArgoCD modu, aynı kümede art arda koşular) + GitHub Actions (taze küme, run 34611395752 ✅; final review düzeltmeleriyle run 34616712300 ✅ `E2E F2 OK`, e2e-kind ≈ 22 dk). pg yolu: fixture → Debezium+sink → Bronze (3 I) → MERGE #1 (Silver DDL) → UPDATE/DELETE → Bronze (I3/U1/D1) → MERGE #2 (artımlı, 2 anahtar) → Silver doğru → 3 bakım işi.

| # | Konu | Canlı bulgu | Sonuç |
|---|---|---|---|
| 1 | Sink transactional id ACL'leri | Iceberg sink coordinator/worker producer'ları `coordinator-txn-<uuid>`, `worker-txn-<uuid>` transactional id kullanır; yalnız `connect` prefix'i vardı → task `TransactionalIdAuthorizationException` ile öldü | KafkaUser ACL: `transactionalId` prefix `coordinator-txn-` + `worker-txn-` [Write, Describe] |
| 2 | FAILED task'ın toparlanması | Task FAILED kalır; Strimzi `spec.autoRestart.enabled: true` geri-çekilmeli yeniden başlatır (2, 6, 12 dk…) | Her iki KafkaConnector'da `autoRestart` (F5 alarmı yine gerekir) |
| 3 | Sink ilk commit süresi | Task RUNNING'den ~2 dk sonra control consumer gruba katıldı, ilk gerçek commit ~5 dk sonra; aradaki "committed to 0 table(s)" satırları normal | Runbook: 5 dk beklemeden alarm yok |
| 4 | Spark 4 / Iceberg 1.11 zaman-yolculuğu | `option("snapshot-id")` kaldırıldı: `Time travel option snapshot-id is no longer supported` → merge tam-okuma yolu `versionAsOf` | `merge_cdc.load_bronze` |
| 5 | `remove_orphan_files` | Varsayılan Hadoop FS listelemesi → `No FileSystem for scheme "s3"` (resmi Spark imajında s3a yok) | `prefix_listing => true` (S3FileIO, vended creds) |
| 6 | Artımlı okuma | Snapshot watermark ile `start/end-snapshot-id` çalıştı: MERGE #2 `incremental, 2 anahtar`; tam-okuma yolu yalnız ilk koşuda | P5 doğrulandı |
| 7 | Zamanlanmış merge | `silver-merge` cron (*/30) koşuları COMPLETED (~40 s, packages çözümü dahil); Bronze yokken "atlandı" yolu | — |
| 8 | Fixture sırası (e2e) | Debezium `Secret <name>-db` yoksa connector NotReady → glue Degraded → Application beklemesi tıkanır; CNPG webhook hazır değilken apply reddedilir | run.sh: fixture bootstrap'tan hemen sonra, apply yeniden denemeli |
| 9 | Aynı kümede yeniden koşu | Kök yeniden bootstrap edilince alt Application'ın eski "Synced" durumu yanıltır (ConfigMap güncellenmeden iş koştu); aynı adlı FAILED SparkApplication yeniden koşmaz | run.sh git app'lerin `sync.revision`'ını bekler; `run_spark_once` önce siler. pg-path mutasyona uğramış kaynağa karşı idempotent değil (taze küme tasarımı) |
| 10 | Dev MinIO kalıcılığı | PVC yok → pod yeniden başlayınca bucket sıfırlanır, Polaris'te metadata'sı olmayan tablo kaydı kalır (`table_exists` 400) | smoke toleranslı; **açık:** dev MinIO'ya PVC (final review / F3) |
| 11 | sqlserver şablonu | Yalnız render; `schema.history.internal.*` SASL/PEM yolu (`/opt/kafka/connect-certs/...`) canlı doğrulanmadı | pre-ship OpenShift |
| 12 | Bakım snapshot'ları ↔ artımlı okuma | DÜZELTME: bu etkileşim 30 gün sonra değil BİRİNCİ GÜN görülür — `maint-compact` 6 saatte bir koşar ve append olmayan (replace/delete) snapshot üretir; Bronze TTL delete yalnız en geç örnektir. Artımlı scan bu snapshot'ları atlar, hata verirse yedek okumaya düşer | e2e bakımdan SONRA INSERT + `silver-merge` koşuyor (MERGE #3). **Canlı (CI 34616712300, taze küme):** compaction+expire sonrası MERGE #3 `incremental, 1 anahtar` — Iceberg artımlı append scan replace/delete snapshot'larını atladı, yedek okuma tetiklenmedi. Zaman yolculuğu `--prev-rows 3` ✅ |
| 13 | Yedek okumanın maliyeti | Artımlı okuma başarısızsa eski davranış Bronze'un TAMAMINI okumaktı (30 günlük TTL penceresi büyüdükçe kabul edilemez) | `lakehouse.bronze.max-ts` özelliği: yedek okuma `_cdc.ts >= max-ts - INTERVAL 1 DAY` (day(_cdc.ts) partition pruning). VARSAYIM: geç satırlar 1 gün içinde; daha eskisi yalnız tam okumayla yakalanır (özellik silinerek) |
| 14 | Bakım tablo listesi | Liste `pipelines.json`'dan türetiliyordu: Silver pipeline'ı OLMAYAN (append-only) Bronze tablolar hiç bakım görmüyordu — compaction/expire olmadan sonsuz büyürdü | `pipelines.json`'a `bronze_namespaces` (values.sources'tan); `iceberg_maintenance` Bronze listesini `SHOW TABLES IN <katalog>.<ns>` ile katalogdan kurar |
| 15 | sqlserver TLS varsayılanı | Şablon `database.encrypt=false` gönderiyordu: kaynak DB trafiği düz metin | Varsayılan `database.encrypt="true"` + `database.trustServerCertificate` (varsayılan `"false"`); özel CA laboratuvarı için `sources[].trustServerCertificate: true`, üretimde truststore `extraConfig` ile (runbooks/add-source.md) |

## F3 notu (2026-09-11)
Plan `docs/plans/2026-09-11-lakehouse-v2-f3-mongo-nginx.md`. Canlı kanıt: kind/Podman taze küme, helm modu (pg yolu → bakım hatası → düzeltme → aynı kümede devam; mongo + nginx yolları) + GitHub Actions (taze küme, ArgoCD modu): run 34633288332 (a91d458, 34 dk) ✅ ve run 34636553373 (d1d5220, final fix dalgası) ✅ — üç yol da `E2E … OK`.

| # | Konu | Canlı bulgu | Sonuç |
|---|---|---|---|
| 1 | Debezium mongo ham envelope (schemas.enable=false) | Key `{"id":"{\"$oid\": \"…\"}"}` (JSON içinde JSON string); value `{before, after:<JSON string>, updateDescription, source{ts_ms, snapshot:"true", db, collection, ord…}, op:"r"/"c"/"u"/"d", ts_ms:<int>, transaction}` | `mongo_lib.mongo_id` `$oid` açar; `ts_ms` int; sınıflandırma bronze (Q1/Q3 doğrulandı) |
| 2 | Spark batch `startingOffsets` | Kayıtlı offsets `{0:2,2:1}` (partition 1 kayıt görmedi) → `KafkaIllegalStateException: Partitions specified for Kafka start offsets don't match what are assigned`; Spark TÜM partition'ları ister | `mongo_lib.starting_offsets` eksik partition'a `-2` (earliest); partition listesi `mongo_bronze.topic_partitions` (kafka-clients AdminClient, Spark JVM; paket zaten classpath'te). #2 koşu: `offsets {0:2,1:-2,2:1} -> {0:4,2:2}`, 3 bronze |
| 3 | `mongo-bronze` süresi / şekli | Zamanlanmış koşu 1 dk 47 s (packages çözümü dahil), "3 bronze, 0 karantina, 0 tombstone"; mutasyon sonrası 3 (U, D, I); Bronze 6, Silver `incremental, 3 anahtar` → 3 satır (`_doc` platinum, Bob silindi, Deniz eklendi); karantina 0 | Q2 batch+offset deseni geçerli; Silver mongo için de artımlı |
| 4 | Zamanlanmış `mongo-bronze` | Dev `*/30` cron kendiliğinden koştu ve Bronze'u üretti; sonraki silver-merge crm'i de aldı | mongo-bronze append idempotent DEĞİL: e2e tek seferlik koşusu :00/:30 ile çakışırsa Bronze çift satır (Silver dedup düzeltir). Dev cron korundu (silverMerge ile aynı); e2e flake görülürse run.sh SSA `suspend` |
| 5 | Bakım TTL kolonu | `bronze_namespaces` listesi `crm_raw.customers__quarantine` düz tablosunu (üst düzey `ts`) da getirdi → `DELETE … _cdc.ts` `UNRESOLVED_COLUMN` | `iceberg_maintenance`: `_cdc` kolonu varsa `_cdc.ts`, yoksa `ts` (şemadan) |
| 6 | `connect` KafkaUser ACL — nginx | `sink-nginx` task: `Could not initialize dead letter queue topic=nginx.dlq … TopicAuthorizationException` (prefix ACL'ler yalnız sources'tan) | `nginx.enabled` iken topic prefix `nginx.` [Read, Write, Describe, Create]; autoRestart + `strimzi.io/restart-task=0` ile 10 s'de RUNNING |
| 7 | Fluent Bit 5.1.2 → Kafka | Küme içi ajan (aynı config, SASL_SSL/SCRAM + PEM CA) 3 satırı yazdı; kayıt `{"_fb_ts":1789134744.0,"remote","host","user","method","path","code":200,"size":512,"referer","agent","ts":1789134744000}` (lua `ts` tam sayı ms; `Types` ile code/size int) | Q5 doğrulandı: sink `TimestampConverter unix/milliseconds` → `ts` timestamp; verify `~ts=2026-09-11 13:52:24` (saniye doğruluğu) |
| 8 | nginx sink commit | Task RUNNING → ~5 dk içinde commit; consumer lag 0; DLQ boş | F2 notu 3 ile aynı süre |
| 9 | Dış listener (kind) | `lakehouse-kafka-external-bootstrap` NodePort 30731; e2e yalnız varlığını doğrular, ajan küme içinden 9093'e bağlanır | OpenShift Route yolu pre-ship ortamında |
| 10 | Debezium mongo yetkileri | Dev fixture `root`; replica set `rs0` tek düğüm (change streams şartı); `demo-mongo-init` Job idempotent | Prod en az yetki `runbooks/add-source.md` (read <db>, read config, clusterMonitor) |
| 11 | e2e harness | (a) verify Job komutu `sh -c` içinde boşluklu koşulu böldü → `lib.sh` her argümanı tek tırnaklar; (b) `~k=…13:52:24` ':' değer içinde → `cond` '=' içermeyen parçayı önceki değere ekler; (c) `run_spark_once --tail=200` iki pipeline'da ilk pipeline'ın satırını düşürdü → 2000 | — |
| 12 | Kafka partition ↔ dedup | Debezium mongo topic'i 3 partition; kayıtlar `_id` ile keyed → aynı doküman aynı partition → Silver `_cdc.offset` tiebreak geçerli | — |
| 13 | nginx_raw bakım kapsamı | `maintain_namespaces` ile compact/expire-orphan bakımına alındı (TTL uygulanmaz) | saklama kararı F5 |

Açık kalanlar (F4/F5): `nginx_raw.access_log` bakım kapsamında (satır 13: `maintain_namespaces`, compaction/expire/orphan) ama TTL yok → saklama kararı (F5); karantina TTL 30 gün (bronzeTtlDays) uygun mu; DLQ `context.headers.enable` ortak sink helper'da tek karar (F4); Route dış listener pre-ship; ajan gerçek sunucuda kurulum; mongo incremental snapshot sinyali canlı test edilmedi; dev MinIO PVC; `_cdc.source` mongo'da topic adı (pg'de kaynak tablo id'si) — belgelenmiş fark; `_cdc.ts` mongo'da envelope `ts_ms` (connector işleme zamanı), `_cdc.offset` Kafka offset'i; pg'de DebeziumTransform kaynak-türevli — `day()` partition, TTL ve bounded-fallback penceresi bu kolona bağlı.

## F4 notu (2026-09-17)
Plan `docs/plans/2026-09-17-lakehouse-v2-f4-user-facing.md`. Bileşen bazlı canlı kanıt Task 3–6'da aynı kind kümesinde toplandı (dört F4 yolu tek tek `E2E F4 … OK`); bu notun 1–14. satırları o koşulardan, 15–18. satırlar Task 7'nin taze-küme koşularından gelir. Taze küme uçtan uca doğrulaması: kind/Podman (helm modu) **54 dk 14 sn** (23:58:25 → 00:52:39, 2026-09-17/18) — sekiz işaret sırayla: `E2E F1 OK`, `E2E F2 OK`, `E2E F3 MONGO OK`, `E2E F3 NGINX OK`, `E2E F4 TRINO OK`, `E2E F4 SUPERSET OK`, `E2E F4 JUPYTERHUB OK`, `E2E F4 ZEPPELIN OK`. Bu yeşil koşuya ulaşmak 15–18. satırlardaki dört düzeltmeyi gerektirdi. CI (ArgoCD modu, taze runner) **YEŞİL: run 35285423931** (`e66e4da`, `e2e-kind` işi **43 dk 9 sn**, aynı sekiz işaret). Yeşile ulaşmak beş deneme aldı; her biri gerçek bir kusur ortaya çıkardı (aşağıdaki tablo).

| # | Konu | Canlı bulgu | Sonuç |
|---|---|---|---|
| 1 | Keycloak token (dev) | `iss=http://keycloak-service.lakehouse.svc:8080/realms/lakehouse`, `aud=trino` (tek öğe → **skaler**, liste değil), `azp=trino`, `preferred_username=student1` | `hostname` tam URL (v2 API); audience mapper olmadan Trino Bearer'ı reddeder; realm sırları `spec.placeholders` ile Secret'tan; realm import mevcut realm'i güncellemez |
| 2 | Trino probe davranışı | HTTPS + kimlik doğrulama açıkken bile `/v1/info` kimliksiz servis ediliyor (liveness `http://:http/v1/info`, readiness/startup `exec health-check`) → pod 1/1, restart 0 | `allow-insecure-over-http` KULLANILMADI (kullanılsaydı Basic reddedilir, Bearer yok sayılırdı) |
| 3 | Helm map birleştirmesi | `coordinator.additionalConfigFiles: {}` prod `group-provider.properties`'i SİLMEZ (`grep -c group-provider.name=ldap` → 1; `null` verilince → 0) | LDAP group provider ayrı dosyada: `platform/values/trino-ldap.yaml` (yalnız prod `valueFiles`); dev overlay listeyi `[trino.yaml, trino-dev.yaml]` ile replace eder |
| 4 | Trino chart 8443 | `server.config.https.enabled: true` iken chart 8443/`https`'i container'a ve Service'e zaten ekliyor; `coordinator.additionalExposedPorts` ikinci kez eklediğinde API `spec.ports[2].name: Duplicate value: "https"` ile reddediyor | `additionalExposedPorts` kaldırıldı; Service tam olarak `8080 http` + `8443 https` |
| 5 | Trino 483 S3 özelliği | Log: `'fs.native-s3.enabled' has been replaced. Use 'fs.s3.enabled' instead` | Her iki values dosyasında `fs.s3.enabled` |
| 6 | Polaris DROP + purge | Trino REST katalog `DELETE …?purgeRequested=true` gönderiyor; Polaris 403: `Unable to purge entity … set DROP_WITH_PURGE_ENABLED` (yetki sorunu DEĞİL) | `platform/values/polaris.yaml` → `features.DROP_WITH_PURGE_ENABLED=true`; **sunucu geneli**, kabul edilen risk (katalog düzeyi `properties` var olan katalogda `setup apply` ile güncellenmiyor) — `runbooks/access-control.md` |
| 7 | Superset operator 0.2.0 CRD şekli | `podTemplate` bir `PodTemplateSpec` DEĞİL: tek isimsiz `container`; global `spec.podTemplate` lifecycle Job'larına da uygulanır ve bileşen şablonuyla toplanarak birleşir. CEL: `secretKey`, `metastore.uri`, `lifecycle.init.adminUser` yalnız `Development`'ta. Deployment/Service/ConfigMap adı `<CR>-web-server` (8088), pod etiketi `instance=<CR>` | OIDC/Trino env + CA global şablonda (`spec.config` migrate/init Job'larında da değerlendirilir, `os.environ[...]` orada da çözülmeli); admin kullanıcı yok → yönetici `AUTH_ROLES_MAPPING` ile OIDC'den gelir |
| 8 | Superset imajı | `apache/superset:6.1.0` hiçbir DB sürücüsü içermiyor (psycopg2 dâhil) ve venv site-packages salt-okunur (uid 1000) → açılışta pip sessizce etkisiz. Resmi **`6.1.0-dev`**: psycopg2-binary 2.9.9, trino 0.330.0, authlib 1.6.7 hazır; gunicorn konteyner başlangıcıyla **aynı saniyede** dinlemede (pip'li sürümde 29 s) | `imageTag: 6.1.0-dev`, `bootstrapScript` YOK, startupProbe ezilmiyor (operator varsayılanı 15s+3×10s yeter); varsayılan repo Scarf yönlendiricisi → prod'da aynaya sabitlenmeli |
| 9 | TLS güven ayrımı | Superset'e `REQUESTS_CA_BUNDLE=lakehouse-ca` koymak prod'da Keycloak'ı (edge Route, router sertifikası) doğrulanamaz yapardı; dev bunu göremez (Keycloak düz HTTP). Env kaldırıldıktan sonra Trino TLS yalnız datasource `connect_args.verify` ile doğrulandı, e2e geçti | Superset: sistem kökleri + datasource `verify`. JupyterHub: **hub** pod'unda CA daraltması yok, **singleuser** pod'unda `REQUESTS_CA_BUNDLE` (yalnız Trino için) |
| 10 | kind + NetworkPolicy | kindnetd `v20260820-69b56db7` NetworkPolicy'yi **ENFORCE EDİYOR**: z2jh varsayılanı özel IP'lere çıkışı kapattığından notebook → Polaris/Trino `Connection timed out` (SYN_SENT) | `singleuser.networkPolicy.egress` podSelector ile Trino 8443 + Polaris 8181 (+dev MinIO 9000); `egressAllowRules.privateIPs` (tüm RFC1918) KULLANILMADI. Repodaki "kind enforce etmez" yorumları düzeltildi |
| 11 | z2jh spawn | `pyspark-notebook:spark-4.1.2` soğuk çekim **273,8 s** (~1,94 GB) → varsayılan `start_timeout` 300 s yetmiyor ("failed to start in 300 seconds, giving up"). `enable_auth_state` açıkken admin API ile yaratılan kullanıcı `refresh_user()` → `auth_state` yok → 403 | prod `singleuser.startTimeout: 1200`; dev `Authenticator.auth_refresh_age: 0` (yalnız e2e; gerçek OAuth kullanıcısını etkilemez). Ilık imajda spawn ~21 s. e2e'de `user delete 400 "Cannot delete yourself!"` (servis ve kullanıcı adı aynı: `e2e`) — zararsız, kayıt kalıcı, sonraki koşuda `create 409` |
| 12 | Zeppelin `config.fs.dir` | `ZEPPELIN_CONFIG_FS_DIR=/data/conf` tek başına yetti (`zeppelin-site.xml` ConfigMap'i gerekmedi): tohumdaki 1 interpreter okunuyor, kalan 23'ü şablonlardan tamamlanıyor, dosya PVC'de kalıcı | initContainer `cp` yalnız dosya yokken; Secret'ı güncellemek TEK BAŞINA etkisiz (`runbooks/user-facing.md`) |
| 13 | Zeppelin çalışma modu / bağımlılık | `ZEPPELIN_RUN_MODE` varsayılanı (`auto`) küme içinde k8s modunu seçip interpreter için ayrı pod açmayı deniyor → `pods … is forbidden` (default SA). `trino-jdbc` indirmesi **açılışta ve asenkron**: 64 s boyunca paragraf `Interpreter Setting 'jdbc' is not ready … DOWNLOADING_DEPENDENCIES` ile düşüyor. `POST /api/notebook` 0.12.1'de `name` alanını yok sayıyor (her zaman `/Untitled Note`) | `ZEPPELIN_RUN_MODE=local`; e2e `jdbc.status == READY` bekliyor ve `/Untitled Note`'u önce siliyor. Helm `tpl` kaçışı: Zeppelin'in kendi `{{applicationId}}`'si `{{ "{{applicationId}}" }}` ile kaçırıldı |
| 14 | rules.json canlı davranış | `analyst1`: shop.orders 3 satır, maskesiz `remote`, sandbox CTAS+DROP başarılı · `student1`: 2 satır (`status <> 'shipped'`), `remote='x.x.x.x'`, sandbox CTAS Access Denied · servis hesabı `e2e`: okur, sandbox'a yazamaz | İlk eşleşen kural kazanır; servis hesapları satır/kolon kurallarını **tasarım gereği** atlar (spec §14) → Superset/Zeppelin'de kullanıcı bazlı maskeleme yok |
| 15 | **Taze küme yarışı: CNPG** | glue'nun tüm nesneleri aynı anda uygulanıyor; üç CNPG Cluster'ı ~4 dk 14 sn sonra hazır oluyor. `job/polaris-bootstrap` 6 denemeyi (~10 dk) tüketip `BackoffLimitExceeded`; Superset operator'ün Migrate görevi 3 denemeyi **14 sn içinde** tüketip KALICI `TaskFailed` (psycopg2 `connection refused`) | `polaris-bootstrap` `backoffLimit: 12`; `Superset.spec.lifecycle.migrate.maxRetries: 120`. ArgoCD modunda sync-wave'ler (Argo'nun CNPG Cluster health check'i) bu yarışı zaten örtüyor |
| 16 | **Erken Spark cron'ları belleği yiyor** | dev `silverMerge`/`mongoBronze` `*/30` cron'u polaris-setup'tan (Secret `polaris-spark`) önce fırlıyor; driver pod'ları `CreateContainerConfigError`'da kalıyor ama her biri 1536m+512m = 2 GiB **rezerve ediyor** → düğüm istekleri %91, `connect-connect-0` (1536Mi) "0/1 nodes are available: 1 Insufficient memory" ile 16 dk Pending | dev schedules gecelik yapıldı (`silverMerge 0 3`, `mongoBronze 5 3`); e2e zaten `run_spark_once` kullanıyor. F3 notu 4'teki cron çakışması riski de kapandı. Prod schedules değişmedi |
| 17 | **Sıralı imaj çekimi** | Taze kind düğümünde kubelet imajları SIRAYLA çekiyor: `apache/zeppelin:0.12.1` (2,7 GB) 5 dk 11 sn; kuyrukta bekleyen CNPG postgres imajı yüzünden `polaris-db`/`superset-db` initdb pod'ları 11+ dk `PodInitializing` → `helm upgrade glue --wait --timeout 25m` `context deadline exceeded`; CI'da (ArgoCD) `application/glue` 1800 s içinde Healthy olmadı | `test/e2e/kind.sh` kind config'i: `serializeImagePulls: false`, `maxParallelImagePulls: 4`; `bootstrap.sh` glue `--wait 40m`, polaris-bootstrap beklemesi 900 s; `run.sh` ArgoCD modunda `wait_app glue 1 2700s` |
| 18 | **Lokal (macOS/podman) kaynak tavanı** | Podman VM 13 GiB / host 16 GB: F4 yığını + paralel imaj çekimi host'u takasa sokuyor (`PhysMem … 114M unused`, compressor 8,9 GB, load avg 546) → imaj çekimi 29 dk asılı kaldı, API server RBAC'ı geçici olarak "forbidden" döndürdü, kubelet pod'ları Unknown'a düşürdü | 17. satırdaki paralel çekim + 15/16. satırlardaki düzeltmelerden sonra tam koşu bu makinede de yeşil (54 dk); ama marj dar: host başka iş yaparken (tarayıcı, senkronizasyon istemcisi) koşu asılabiliyor. Lokal geliştirmede tek tek bileşen yolları (Task 3–6 kalıbı) tercih edilmeli |
| 19 | **ArgoCD: WaitForFirstConsumer PVC wave'i kilitliyor** | `application/glue` 45 dk `OutOfSync/Progressing`; controller log'u: `waiting for healthy state of /PersistentVolumeClaim/zeppelin-data`. Wave 0'daki tüm görevler `Succeeded`, tek istisna PVC `(Synced,Running)`; wave 1–3 kaynakları (`polaris-bootstrap` hook, KafkaConnect, Keycloak, `trino-tls`, Zeppelin Deployment, Superset CR, connector'lar, SSA'lar) `nil->obj (,,)` — hiç uygulanmadı. Neden: kind'ın `standard` StorageClass'ı `WaitForFirstConsumer`; PVC'yi bağlayacak pod (Deployment, wave 2) hiç yaratılmıyor → kilitlenme. (`trino` Application'ının `Degraded`'ı sonuçtur: coordinator `trino-tls`'i bekliyor.) | PVC'ye Deployment'la **aynı** wave (`argocd.argoproj.io/sync-wave: "2"`); unittest ikisinin de wave 2 olduğunu doğrular. Helm modunda wave kavramı olmadığı için lokal koşu bu hatayı göremez — ArgoCD'ye özgü sınıf: **geç wave'de tüketilen her PVC tüketicisiyle aynı wave'de olmalı** |
| 20 | **CI runner'ında CPU isteği tükeniyor** | Run 35281673277: `E2E F1 OK` sonrası `e2e-silver-merge-driver` **20 dk `Pending`**, SparkApplication `SUBMITTED`'da asılı. Düğüm istekleri `cpu 3060m (%76)` / `memory 7624Mi (%47)` — 4 vCPU'luk runner'da 1 çekirdeklik driver (+ 1 çekirdeklik executor) sığmıyor. Lokalde (8 vCPU) aynı istekler %38 olduğu için sorun görünmüyor | `spark-jobs.yaml`'a `coreRequest` (spark-operator `SparkPodSpec.coreRequest`; `cores` Spark çekirdeği olarak 1 kalır) eklendi ve dev'de driver/executor **500m**, Trino coordinator **250m**, Zeppelin/Superset **100m** istek verildi (limitler aynı). Yeşil koşuda düğüm: `cpu 2610m (%65)` / `memory 7624Mi (%47)`, 36 pod. Prod render'ında `coreRequest` YOK |
| 21 | **Zeppelin rol filtresi giriş yolunda NPE** | Final fix dalgası C1: `/** = authc, anyofroles[admin, analyst, student]` eklenince `/api/login` **500** verdi. `org.apache.zeppelin.utils.AnyOfRolesUserAuthorizationFilter.isAccessAllowed` (0.12.1) rolü bulamazsa `subject.getPrincipal().equals(rol)` çağırıyor; Shiro'nun `authc` filtresi loginUrl'de BAŞARISIZ girişten sonra da zinciri sürdürdüğü için (asıl girişi Zeppelin'in `LoginRestApi`'si yapar) filtre kimliksiz subject görüyor -> `NullPointerException` | Rol kapısı giriş/çıkış yolundan muaf: `[urls]`'e `/**`'dan ÖNCE `/api/login = authc` + `/api/login/** = authc`. Diğer TÜM yollarda authc kimliği garanti ettiği için NPE olmuyor. Canlı: `analyst1` giriş 200 + `/api/notebook` 200, yanlış parola 403, **rolsüz `nogroup1` giriş 200 ama `/api/notebook` 401** (delik kapandı). Sınıf adı brief'te `AnyOfRolesAuthorizationFilter` yazıyordu; imajdaki gerçek ad `AnyOfRolesUserAuthorizationFilter` (jar listesinden doğrulandı) |
| 22 | **Zeppelin imajının soğuk çekimi progress deadline'ı aşıyor** | Taze kümede kubelet `apache/zeppelin:0.12.1` (~2,7 GB) için `Successfully pulled … in 13m48s` yazdı; Deployment'ın k8s varsayılanı `progressDeadlineSeconds: 600` dolduğu için `Progressing=False/ProgressDeadlineExceeded` oldu ve `helm upgrade --wait` (bootstrap.sh) kurulumu "Deployment/lakehouse/zeppelin not ready" diye DÜŞÜRDÜ — pod aslında sağlıklıydı (Shiro ortamı yüklendi, "zeppelin server started") ve hemen sonra Ready oldu | `glue/templates/zeppelin.yaml` -> `progressDeadlineSeconds: 1800` (+ unittest). 17. satırdaki paralel çekim ayarı çekimi hızlandırıyor ama tek başına yetmiyor: sorun çekim süresi değil, Deployment'ın sabrı |
| 23 | **Polaris purge bayrağı katalog düzeyinde çalışıyor** | I6 ruling'i (dene): sunucu geneli `features.DROP_WITH_PURGE_ENABLED` yerine `lakehouse` kataloğunun `properties`'inde `polaris.config.drop-with-purge.enabled: "true"`. Katalog düzeyi anahtar adı `apache/polaris:1.7.0` içindeki `polaris-core` jar'ından doğrulandı (`FeatureConfiguration` string sabitleri: `DROP_WITH_PURGE_ENABLED` <-> `drop-with-purge.enabled` <-> `polaris.config.drop-with-purge.enabled`) | **POZİTİF.** Taze kümede katalog bu property ile yaratıldı (`polaris catalogs get lakehouse` -> `"polaris.config.drop-with-purge.enabled": "true"`), canlı `setup apply --dry-run` şemayı kabul etti ve polaris Deployment'ında purge env'i YOK; son kanıt Trino sandbox CTAS+DROP (trino-check). Kurulum uyarısı: `setup apply` katalog properties'ini yalnız katalog YARATILIRKEN yazar → var olan kurulumda `polaris catalogs update --set-property …` gerekir (runbooks/install.md F3→F4 bölümü) |


Lokal yeşil koşunun kaynak fotoğrafı (kind düğümü, sekiz yol bittiğinde, 32 pod):
```
Allocated resources:
  Resource           Requests      Limits
  cpu                3060m (38%)   2200m (27%)
  memory             7624Mi (51%)  12566Mi (84%)
```
Not: aynı koşuda `superset-migrate` Job'ının İLK denemesi yine `Error` verdi (CNPG henüz hazır değil) ve operator
yeniden deneyerek başarıya ulaştı — 15. satırdaki `maxRetries: 120` düzeltmesinin canlı kanıtı.

CI kaynak fotoğrafı (**yeşil** run 35285423931, `if: always()` adımı, 36 pod): runner `Mem: 15989 MB toplam / 11807 kullanılan / 3666 cache`, swap 103 MB, disk `/dev/root 145G %64`; düğüm istekleri `cpu 2610m (%65)` / `memory 7624Mi (%47)`, limitler `cpu 2200m (%55)` / `memory 12566Mi (%78)`. Yani 4 vCPU'luk runner'da darboğaz **CPU isteği**, bellek değil; F5 boyutlandırmasında marj buradan hesaplanmalı.

### Final fix dalgası (2026-09-18) — whole-branch review düzeltmeleri

Kapsam: C1/C2 (Zeppelin rol kapısı + not defterleri private), I1 (prod hostname/caBundle yer tutucuları),
I3 (CI'da prod Trino/JupyterHub render'ları), I5 (JupyterHub `REQUESTS_CA_BUNDLE` kaldırıldı), I6 (Polaris purge
bayrağı katalog düzeyine), I2/I4/I7 + F3→F4 yükseltme sırası (belgeleme), minor'lar. Commit'ler: `1014e19`
(güvenlik), `56223be` (prod/CI/e2e), `6bf30e8` (belgeler). Üç yeni canlı bulgu: 21, 22, 23. satırlar.

**Lokal doğrulama yarıda kaldı (kanıt değeri sınırlı):** bu dalga için başlatılan taze kind koşusu iki kez
düştü. (a) İlk düşme 22. satırdaki progress deadline'dı (düzeltildi). (b) İkinci koşu `MERGE #1` adımında
Polaris→MinIO **vended credential** hatasıyla düştü: `ForbiddenException: The Access Key Id you provided does
not exist in our records` (403) — MinIO/Polaris yeniden başlamamıştı; aynı flake 2026-09-14/15'te uzun ömürlü
kümede görülmüştü, **F5 backlog kalemi** (vended credential ömrü/önbelleği) ve bu dalganın değişiklikleriyle
ilgisi yok. Ardından dizüstü ~04:15–09:24 arası uykuya girdi (spark-operator 06:24:34Z'de birikmiş cron SSA'larını
işleyip e2e işini Failed işaretledi). Lokal gece koşuları bu makinede güvenilir değil (18. satır).
Bu nedenle **bu dalganın otoriter taze-küme kapısı CI'dır** (ArgoCD modu): `trino-check` CTAS+DROP ile I6'nın
katalog düzeyi purge property'sini, `zeppelin-path` yeni Shiro `anyofroles` kapısını ve
`ZEPPELIN_NOTEBOOK_PUBLIC=false` altında not yaratma/çalıştırmayı, `jupyterhub-path` de `REQUESTS_CA_BUNDLE`
olmadan açık `verify=` yolunu koşturur.

**CI YEŞİL: run 35315068741** (`6bf30e8`, `e2e-kind` **43 dk 25 sn**, `helm-unittest` ✅ — I3'ün iki yeni prod
render adımı dâhil). Sekiz işaret sırayla: `E2E F1 OK` (06:44) · `E2E F2 OK` (06:58) · `E2E F3 MONGO OK` (07:04) ·
`E2E F3 NGINX OK` (07:09) · `E2E F4 TRINO OK` (07:09) · `E2E F4 SUPERSET OK` (07:10) · `E2E F4 JUPYTERHUB OK`
(07:11) · `E2E F4 ZEPPELIN OK` (07:11). Bu dalgaya özgü satırlar:
`OK analyst1 sandbox CTAS` + **`OK analyst1 sandbox DROP`** (I6: sunucu bayrağı YOKKEN katalog property'siyle
purge'lü DROP geçti) · **`OK zeppelin rol kapısı: rolsüz kullanıcı 401 (giriş yapsa bile veri göremez)`** (C1) ·
`user create 201` / `server start 202` / `server stop 204` (minor: yeni HTTP kodu assert'leri; I5: JupyterHub
`REQUESTS_CA_BUNDLE` olmadan çalışıyor). Docs commit `5d03c58` için koşan run **35315187719** de ✅.

Lokal olarak **doğrudan** kanıtlanan parçalar (aynı taze kümede, ölçüldü): Zeppelin rol kapısı
(`analyst1` 200/`/api/notebook` 200, yanlış parola 403, rolsüz `nogroup1` 200 ama `/api/notebook` **401**),
`ZEPPELIN_NOTEBOOK_PUBLIC=false` pod env'inde, Polaris kataloğunun `polaris.config.drop-with-purge.enabled`
property'si (`catalogs get`) + canlı `setup apply --dry-run` + polaris Deployment'ında purge env'inin YOKLUĞU,
`E2E F1 OK` (polaris-smoke vended yaz/oku).

CI denemeleri (hepsi `helm-unittest` işi ✅; `e2e-kind` işi):

| # | Run | Commit | Sonuç | Kök neden → düzeltme |
|---|---|---|---|---|
| 1 | 35267164775 | `0d2be72` | ❌ | `application/glue` 1800 s'de Healthy olmadı (sıralı imaj çekimi) → `91bea7c` |
| 2 | 35271184420 | `91bea7c` | ❌ | glue 45 dk `OutOfSync/Progressing`; wave 0'ın bir kısmı uygulanmış → teşhis eklendi + `syncPolicy.retry` (`00dd713`) |
| 3 | 35276782791 | `00dd713` | ❌ | **19. satır**: `waiting for healthy state of /PersistentVolumeClaim/zeppelin-data` → `9c919bd` |
| 4 | 35281673277 | `9c919bd` | ❌ | PVC kilidi çözüldü (glue 7 dk'da Synced, trino/jupyterhub Healthy, `E2E F1 OK`, Bronze tamam); yeni engel **20. satır**: `e2e-silver-merge-driver` 20 dk Pending → `e66e4da` |
| 5 | **35285423931** | `e66e4da` | ✅ | `e2e-kind` 43 dk 9 sn; sekiz işaret sırayla yeşil (`E2E F1 OK` 15. dk → `E2E F4 ZEPPELIN OK` 42. dk) |
| 6 | **35315068741** | `6bf30e8` | ✅ | Final fix dalgası (C1/C2/I1/I3/I5/I6 + progressDeadline + minor'lar); `e2e-kind` 43 dk 25 sn, sekiz işaret sırayla |
| 7 | 35315187719 | `5d03c58` | ✅ | Yalnız F4 notu satırı farklı (kod 6bf30e8 ile aynı) |


Açık kalanlar (F5/pre-ship): tarayıcı OIDC akışları (Superset, Trino Web UI `/ui`, JupyterHub) yalnız pre-ship OpenShift'te canlı doğrulanabilir — dev'de Keycloak hostname'i küme içi URL (e2e Bearer/password grant ile kanıtlıyor); OpenShift Route **reencrypt** (`tls.caBundle` → `destinationCACertificate`) canlı; **LDAP group provider** (`platform/values/trino-ldap.yaml`) AD'ye karşı canlı; sertifika yenilemenin (`http-server.https.ssl-context.refresh-time 1m`) kesintisizliği gözlenmedi; Superset **Alerts&Reports** (Valkey + `celeryWorker` + headless tarayıcı) kapalı; Superset imajı için müşteri **aynası + digest** (varsayılan Scarf yönlendiricisi) ve `-dev` etiketinin taşınması; Trino chart'ının varsayılan `tpch`/`tpcds` katalogları hâlâ yükleniyor (rules.json erişimi engelliyor, yine de gürültü — `catalogs: {tpch: null}` ile kaldırılabilir); e2e hub kullanıcısı `e2e` silinemiyor (`400 Cannot delete yourself!`, servis adıyla çakışma); `run_check_job` ile `verify` yardımcıları benzeşiyor (DRY fırsatı); Superset operator API'si `v1alpha1` → sürüm takibi (`runbooks/versions.md`, F5); servis hesapları satır/kolon kurallarını tasarım gereği atlar (spec §14) — Superset/Zeppelin'de veri kapsamı araç düzeyinde sınırlanmalı; Trino **resource groups** eklenmedi (ihtiyaç kanıtlanmadı).

## F5 notu (2026-09-18)
Plan `docs/plans/2026-09-18-lakehouse-v2-f5-observability-dr.md`. Bileşen bazlı canlı kanıt Task 1–6'da aynı kind kümesinde toplandı; 1–23. satırlar o koşulardan, 24–26. satırlar Task 7'nin taze-küme koşularından, kontrolörün koşu sonrası canlı ölçümlerinden ve CI'dan gelir. **Kapsam kararı (kullanıcı, görev ortasında):** izleme = *boru hattı sağlığı*; Trino/Superset/JupyterHub/Zeppelin uygulama metriği ve dashboard'ı YOK (Task 2b bunları geri aldı). Taze küme uçtan uca doğrulaması ve CI sonucu aşağıdaki "Taze küme…" bölümünde; 25. satır lokal koşunun neden kapanmadığını, 26. satır yeşile giden tek kod kusurunu anlatır.

| # | Konu | Canlı bulgu | Sonuç |
|---|---|---|---|
| 1 | **Metrik adları (otoriter)** | Beklenenler birebir çıktı: `kafka_connect_connector_task_status{connector,task,status}`, `kafka_connect_sink_task_offset_commit_completion_total`, `kafka_consumergroup_lag{consumergroup,topic,partition}`, `kube_customresource_sparkapp_state`, `kube_customresource_sparkapp_termination_time`, `kube_customresource_ssa_last_run`, `spark_application_{success,failure,running,submit}_count`, `spark_application_success_execution_time_seconds_{sum,count}`. Prometheus'ta toplam 2062 metrik adı | e2e `monitoring-path.sh` bunların **beşini** `M()` ile iddia ediyor (connect task status, sink offset commit, consumer lag, KSM üçlüsü, `spark_application_success_count`) — fix turunda **altıncı** olarak `spark_application_success_execution_time_seconds_count` eklendi: o seri `LakehouseSparkRunTooLong`'un TEK girdisi ve varlığı hiçbir yerde kontrol edilmiyordu (upstream yeniden adlandırsa kural sessizce ölür, `/api/v1/rules` yine `health: ok` derdi — boş vektör hata değildir). `spark_application_{failure,running,submit}_count` bilerek iddia edilmiyor (kural girdisi değiller) |
| 2 | **StateSet tuzağı** | `kube_customresource_sparkapp_state` bir **StateSet**'tir: her CR için 10 durumun HEPSİ (0/1 değerle) yayılır → seri VARLIĞI hiçbir şey ifade etmez | Kural ve e2e iddiaları mutlaka `== 1` içerir (`kube_customresource_sparkapp_state{state="FAILED"} == 1`); `termination_time` için `> 0` |
| 3 | **KSM `customResourceState` yerel anahtar** | kube-prometheus-stack 91.4.1 → KSM subchart 8.5.0 `customResourceState.{enabled,config}` sağlıyor; chart hem ConfigMap'i hem `--custom-resource-state-config-file` argümanını üretiyor | Ayrı glue ConfigMap'i GEREKMEDİ; alan yolları `status.applicationState.state`, `status.terminationTime`, `status.lastRun` canlı doğrulandı; RFC3339→epoch dönüşümü çalışıyor (`time() - …_last_run` anlamlı) |
| 4 | **kafkaExporter ayrı PodMonitor gerektirmedi** | `lakehouse-kafka-exporter-*` pod'u `strimzi.io/kind=Kafka` etiketini ve **aynı port adını/numarasını** (`tcp-prometheus`/9404) taşıyor → mevcut `kafka-resources-metrics` PodMonitor onu kendiliğinden yakalıyor (`up` hedef sayısı 2→3) | `Kafka.spec.kafkaExporter` (topicRegex/groupRegex `.*`) yeterli; gerçek gruplar `connect-sink-{shop,shop-coord,nginx,nginx-coord}` → `connect-sink-.*` regex'i hepsini kapsıyor |
| 5 | **Superset'te `/metrics` YOK** | Superset operator `spec.monitoring.serviceMonitor` üretebiliyor ama **Superset 6.1.0'da böyle bir uç nokta yok**: canlı 404, imajda `prometheus_flask_exporter` kurulu değil | `spec.monitoring` BİLEREK yazılmıyor (açık bırakılsa KALICI `down` hedef olurdu); Superset sağlığı KSM'nin Deployment/pod metrikleriyle izlenir |
| 6 | **İzleme kapsamı daraltıldı (kullanıcı kararı)** | Trino jmx exporter + ServiceMonitor, JupyterHub hub ServiceMonitor (+`authenticate_prometheus`/netpol override'ları), Zeppelin ServiceMonitor (+`ZEPPELIN_METRIC_ENABLE_PROMETHEUS`) ve Trino Grafana dashboard'u (20208) **geri alındı** | Kalan hedefler: `kafka-resources-metrics` (3), `spark-operator-podmonitor` (2), `kube-state-metrics`, `polaris-mgmt` (chart'ın kendi ServiceMonitor'ü), monitoring'in kendi bileşenleri. Stale `down` hedef YOK. Yan fayda: `bitnamilegacy/jmx-exporter` bağımlılığı düştü |
| 7 | **`monitoring` Application sync-wave `-1`** | `platform/apps/dev/40-monitoring.yaml` ile `00-spark-operator.yaml` aynı wave'de (0) olsaydı eşzamanlı sync edilirdi; spark-operator artık `prometheus.podMonitor.create: true` diyor → CRD'ler gelmeden "no matches for kind PodMonitor" | wave `-1`; kustomize çıktısında sıra: `-1 monitoring` → `0 operatörler` → `1 glue` → `2 jupyterhub/polaris/trino`. Kustomize load restrictor nedeniyle dosya `platform/apps/dev/` DİZİNİ altında (tek dosya referansı reddediliyor) |
| 8 | **e2e'nin kendi `*/1` SSA'sı + askıya alma** | KSM CR metrikleri ancak ilgili CR VARSA üretilir; `lib.sh:run_spark_once` SSA'yı askıya alır ve ürettiği SparkApplication'ı siler → taze kümede `sparkapp_*`/`ssa_last_run` BOŞ. Mevcut SSA'nın `schedule`'ını `*/1`'e çekmek İŞE YARAMIYOR: **spark-operator 2.5.2 `status.nextRun`'ı yeniden hesaplamıyor** (2 bağımsız denemede 6 dk koşmadı) | `monitoring-path.sh` `maint-position-deletes` şablonundan `e2e-monitoring` adlı YENİ SSA yaratıyor; İLK koşu yakalanır yakalanmaz `suspend: true` (aksi hâlde dakikada bir driver+executor çifti açılır ve `successfulRunHistoryLimit: 3` iddianın dayandığı CR'ı budar — canlı 3 dk gözlemle doğrulandı). Tek koşu üç metrik ailesini de üretir |
| 9 | **5 kural, hepsi absence-safe** | `LakehouseConnectTaskFailed` (`…task_status{status="failed"} == 1`, 5m) · `LakehouseSinkStalled` (lag tabanlı: `sum by (consumergroup,topic)(kafka_consumergroup_lag{consumergroup=~"connect-sink-.*"}) > 1000`, 15m) · `LakehouseSilverMergeStale` (`max()` boş vektörde ateşlenmez; dev eşik 172800) · `LakehouseSparkScheduledRunFailed` (`…sparkapp_state{state="FAILED"} == 1`) · `LakehouseSparkRunTooLong` | `/api/v1/rules` 5/5 `health: ok`; alertmanager dev'de KAPALI → doğrulama `/api/v1/rules` + `/api/v1/alerts` üzerinden. Runbook anchor'ları (`#connect #sink #silver-merge #spark #spark-duration`) `runbooks/troubleshooting.md` ile birebir eşleşiyor |
| 10 | **`LakehouseSparkRunTooLong` küme geneli ORTALAMA** | spark-operator 2.5.2 exporter'ı **per-app etiket yaymıyor** (tüm `spark_*` serilerinin label seti: `app_type, container, endpoint, instance, job, namespace, pod` — `name`/`app_name` YOK); KSM tarafında da yalnız `termination_time` var, başlangıç zamanı yok | `rate(spark_application_success_execution_time_seconds_sum[6h]) / rate(…_count[6h]) > 1800`, `for: 10m`. Tek yavaş koşuyu DEĞİL, son 6 saatteki ortalamayı yakalar (ham `_sum/_count` ömür-boyu ortalamaydı ve geçmiş biriktikçe kör kalırdı — review'da düzeltildi). `rate()` boş pencerede sonuçsuz → NaN → kural absence-safe |
| 11 | **Barman Cloud eklentisi: chart 0.8.0 = eklenti v0.15.0** | `helm search repo cnpg/plugin-barman-cloud --versions`: chart **0.8.0** → appVersion **v0.15.0**; `--version 0.15.0` diye bir chart YOK | Plan metnindeki "0.15.0 chart" hatası 5 yerde düzeltildi; `platform/apps/00-cnpg-barman.yaml` chart 0.8.0'a sabit |
| 12 | **`ObjectStore.spec.retentionPolicy` `configuration` ALTINDA DEĞİL** | `kubectl explain objectstore.spec --recursive`: `retentionPolicy` **spec** seviyesinde; `s3Credentials.region` düz string değil **Secret referansı** | `spec.retentionPolicy: "30d"`; region referansı kullanılmadı (MinIO'da gerekmedi, prod'da Secret'a `AWS_REGION` eklenip referanslanır). `destinationPath: s3://<bucket>/cnpg` (sondaki `/` YOK — çift eğik çizgi üretmemek için) |
| 13 | **ScheduledBackup etiketi + 6 alanlı cron** | CNPG üretilen Backup'a `cnpg.io/scheduled-backup=<ad>` koyuyor (`cnpg.io/cluster`, `cnpg.io/immediateBackup` de var); `schedule` robfig/cron **6 alan** (saniye önce) → `0 0 2 * * *` | e2e selector'ü (`-l cnpg.io/scheduled-backup=polaris-db-daily`) doğrulandı; eklenti pg pod'una **native sidecar** (`initContainers[].restartPolicy: Always`) olarak enjekte ediliyor → pod 2/2 ama `exec … -c postgres` doğru |
| 14 | **Polaris şeması `polaris_schema`** | Restore doğrulaması `public`'te tablo bulamazdı: Polaris tablolarını `polaris_schema` şemasına yazıyor (canlı, 8 tablo) | `dr-path.sh` sistem şemaları (`pg_catalog`,`information_schema`) dışındaki TÜM tabloları sayıyor |
| 15 | **CNPG yedek/restore süreleri (dev)** | `ContinuousArchiving=True` (3 küme); `immediate` yedekler **8–12 sn** (polaris 10 sn, keycloak 12 sn, superset 8 sn), hepsi `method: plugin`; `polaris-db-restore` kümesi apply → `condition=Ready` **~35 sn**; `dr-path.sh` uçtan uca 53 sn (CNPG yarısı) | Restore yolu `test/e2e/cnpg-restore.yaml` (`externalClusters[].plugin.parameters.{barmanObjectName,serverName}`, `plugins` BİLEREK yok) |
| 16 | **Velero sürümleri + aynı bucket iki prefix** | chart **12.1.0** = Velero **1.18.1**, `velero-plugin-for-aws:v1.14.2` (uyumluluk matrisi v1.14.x ↔ 1.18.x); uploader Kopia; BSL `default` ~46 sn'de `Available` | `s3://backups/cnpg/` (Barman, `retentionPolicy: 30d`) ve `s3://backups/velero/` (Velero, `ttl: 720h`) — Barman retention yalnız kendi prefix'ine dokunur; S3 tarafında lifecycle kuralı YOK (prod'da müşteri S3'ünde iki prefix ayrı ele alınmalı) |
| 17 | **`excludedResources: [pods]` fs-backup'ı TAMAMEN öldürür** | Canlı A/B: `pods` dışlanmış ns yedeği → 734 öğe, **0 PodVolumeBackup**; `pods` dâhil → 809–853 öğe, **22 PodVolumeBackup**. Neden: Velero fs-backup'ı POD öğesi yedeklenirken tetikliyor | Kontrolör kararındaki `pods` listeden ÇIKARILDI; kalan `[replicasets, events, backups.postgresql.cnpg.io]` values'a taşındı ve unittest'le kilitlendi |
| 18 | **Kafka veri hacmi adı `data-0`; pod hacim adı ≠ PVC adı** | `lakehouse-dual-role-0` volumes: `data-0`(jbod id 0), `kube-api-access`, `strimzi-tmp`, `kafka-metrics-and-logging`, `ready-files`, `rack-volume`. Ayrıca zeppelin `data`→PVC `zeppelin-data`, hub `pvc`→PVC `hub-db-dir`, minio `data`→PVC `minio-data`, CNPG `pgdata` | `backup.velero.io/backup-volumes-excludes` **pod hacim adını** alır: Kafka `data-0` (KafkaNodePool `template.pod.metadata.annotations`), CNPG `pgdata` (`inheritedMetadata`), MinIO `data`. e2e bu üç dışlamayı hem annotation hem PVB listesi üzerinden iddia ediyor |
| 19 | **kind sınırı: local-path = hostPath → PVC içeriği fs-backup'a GİRMEZ** | Backup log'u: `Volume data in pod lakehouse/zeppelin-… is a hostPath volume which is not supported for pod volume backup, skipping` (hub `pvc` için de aynı) | kind'da PVB üreten hacimler yalnız **emptyDir**'ler (22 adet: `strimzi-tmp`, `scratch-data`, `shm`, `plugins`, `temp-dir`, `serving-certs`…). Prod'da OpenShift **CSI** gerekir; tercih edilen yol CSI snapshot + Data Mover (`snapshotVolumes`/`snapshotMoveData`). PVC içerik yedeği **pre-ship doğrulaması** — `runbooks/dr.md` bunu açıkça ayırıyor |
| 20 | **İki Velero tuzağı** | (a) `kubectl get backup` kısa adı **CNPG'nin** `backups.postgresql.cnpg.io`'suna çözülüyor (her iki CRD de kurulu) → `kubectl -n velero get backup <ad>` sessizce NotFound döndürüyordu, e2e 10 dk boşa bekledi. (b) `kubectl delete backups.velero.io` nesneyi **S3'ten silmez**: aynı adla ikinci yedek `backup already exists in object storage` ile `Failed` olur | (a) her yerde tam ad: `backups.velero.io`, `restores.velero.io`, `podvolumebackups.velero.io` (CI teşhis adımı ve runbook dâhil). (b) e2e önce **`DeleteBackupRequest`** uygular ve CR'ın kaybolmasını bekler → ikinci koşu idempotent. Velero backup **2 dk 22–26 sn**, marker restore **8 sn** |
| 21 | **Velero Secret'ı chart'tan gelmeli (ArgoCD kilitlenmesi)** | glue'nun `Schedule`'ı `velero.io/v1` CRD'sine muhtaç → velero glue'dan ÖNCEki wave'de olmalı; ama Secret glue'dan gelseydi velero Deployment'ı `ContainerCreating`'de kalır → Application Healthy olmaz → wave 1'e (glue) hiç geçilmez → Secret hiç yaratılmaz. Ters sıra da çalışmaz ("no matches for kind Schedule") | Secret'ı **chart** üretiyor (`credentials.{useSecret,name: velero-s3-creds,secretContents.cloud}`, DEV-ONLY dosya); ns'i `CreateNamespace=true`/`--create-namespace`. velero wave 0, glue wave 1. Prod'da `velero.enabled: false` + OADP (`openshift-adp`) |
| 22 | **dev NetworkPolicy AÇIK — canlı politika kanıtı** | `platform/values/glue-dev.yaml` → `networkPolicy.enabled: true` (F4'te kindnetd'nin enforce ettiği kanıtlanmıştı). Probe'lar: izin verilmeyen ns (`default`) → polaris `:8181` **`000`/timeout (curl 28)**; izin verilen ns (`monitoring`) → **404** (gerçek HTTP yanıtı, sunucuya ulaştı). Prometheus'un `monitoring` ns'inden `lakehouse` pod'larını scrape etmesi `allow-platform-namespaces` sayesinde sorunsuz | 3 NetworkPolicy render ediliyor (`default-deny-ingress`, `allow-same-namespace`, `allow-platform-namespaces`); izinli liste argocd/cert-manager/cnpg-system/monitoring/**velero**. **apiserver→webhook KIRILMADI** (kind): `apply --dry-run=server` hem SparkApplication hem CNPG Cluster için başarılı → `allow-apiserver`/`apiserverCidr` knob'u EKLENMEDİ. **OpenShift SDN/OVN'de yeniden doğrulanmalı** (apiserver'ın ağ konumu ve politika uygulaması farklı olabilir) |
| 23 | **İki doğrulama artefaktı (kod kusuru değil)** | (a) `helm template trino …` çıktısında `grep -c 'tpch'` → **1** (0 değil): kaynak chart'ın kendi `helm.sh/hook: test` Pod'u komut satırında sabit `SELECT COUNT(*) FROM tpch.tiny.nation` metnini taşıyor — bizim `catalogs` ayarımızdan bağımsız, yalnız `helm test` ile oluşur. Anlamlı kontrol `grep -c 'tpch.properties\|tpcds.properties'` → **0**. (b) `lib.sh:run_spark_once` hedef SSA'yı `suspend: true` yapıyor ama işi bitince **geri açmıyor** (F3 mirası) | (a) kabul/doğrulama komutları `*.properties` desenini kullanmalı. (b) `runbooks/troubleshooting.md#ssa-suspend`: dev/kabul kümesinde `kubectl -n lakehouse patch scheduledsparkapplication <ad> --type=merge -p '{"spec":{"suspend":false}}'` |
| 24 | **Grafana dashboard'ları sidecar ile YÜKLENİYOR (canlı)** | Dashboard'lar glue'dan `grafana_dashboard=1` etiketli ConfigMap olarak geliyor (`grafana-dashboard-strimzi-kafka`, `grafana-dashboard-strimzi-kafka-connect`, ns `lakehouse`), Grafana sidecar'ı `searchNamespace: ALL` ile onları **başka bir namespace'ten** topluyor. Canlı zincir (lokal kind `lakehouse`, salt-okunur port-forward, 2026-09-18): sidecar log'u `Writing /tmp/dashboards/strimzi-kafka-connect.json (ascii)` + `Writing /tmp/dashboards/strimzi-kafka.json (ascii)`; ardından `/api/search?type=dash-db` (admin parolası `monitoring-grafana` Secret'ından) **iki dashboard** döndürdü: `Strimzi Kafka` uid **`7ddce8bf9ea7ddb5`** (`/d/7ddce8bf9ea7ddb5/strimzi-kafka`, tag'ler Kafka/Strimzi) ve `Strimzi Kafka Connect` uid **`646c07d84adaf50d`** | Datasource `__inputs` sihirbazıyla DEĞİL, `templating.list[]` içindeki `{name: DS_PROMETHEUS, type: datasource, query: prometheus}` girdisiyle çözülüyor (JSON'larda `__inputs` yok — canlı doğrulandı). `monitoring-path.sh` bu iki başlığı her koşuda iddia ediyor; CI teşhisine fix turunda `grafana-sc-dashboard` sidecar log'u eklendi (ConfigMap'in var olması sidecar'ın onu ALDIĞINI göstermez). Trino dashboard'u (20208) kapsam kararıyla YOK (6. satır) |
| 25 | **Lokal taze-küme koşusu kapanmadı: Podman VM'de yük tepelerinde kontrol düzlemi/operatör liveness zaman aşımları (bellek tavanı DEĞİL)** | Üç taze deneme üç ayrı yerde kırıldı; kontrolörün koşu sonrası canlı ölçümleri ilk okumayı ("host bellek tavanı") **çürüttü**: (a) VM kernel log'unda açılıştan beri, tüm denemeler dâhil, **host düzeyinde `Out of memory:` kaydı YOK** — tüm kayıtlar `Memory cgroup out of memory`, yani konteynerin kendi limiti; (b) **polaris için OOM kaydı hiç yok** → `exit 137` **kubelet liveness kill'idir** (aynı imza attempt-3'te kube-apiserver'da da var: `failed liveness probe, will be restarted`); (c) yığın tamamen ayaktayken (36 pod Running) `free -m` **5,3 GiB available**; (d) VM `/proc/pressure`: **cpu stall 5735 s** vs **memory 561 s** (io 1023 s) → darboğaz **CPU**, bellek baskısı ~10x daha az; (e) attempt-3 event'leri: etcd readiness `context deadline exceeded`, scheduler `TLS handshake timeout`, KSM/strimzi/grafana liveness timeout, cnpg startup probe — apiserver kaybından sonra tüm operatörler exit 1. Kaskad geçtikten sonra bootstrap **kendiliğinden tamamlandı** (36 pod Running); glue helm release'i yalnız ajan koşuyu kestiği için `pending-install` kaldı | Neden: bootstrap fırtınası ve Spark+MERGE yük tepelerinde 8 vCPU'luk libkrun VM'de probe'lar zaman aşımına uğruyor — **F4 notu 18. satırdaki imzanın aynısı, ama uyku olmadan da tetiklendi** (19:15). **Tek gerçek OOM Grafana'nındır** (cgroup, 13 kez; idle RSS 235 MB + ~14 `gpx_grafana-*` eklenti süreci) ve `d56fa48`'in 384Mi→256Mi trim'i onu **ağırlaştırdı** → fix turunda **768Mi**. Denenmemiş çareler: VM'de swap açmak, dev Spark driver/executor **isteklerini** daraltmak, `retention: 24h → 6h`, probe `initialDelay/timeout` gevşetmek. ("≥20 GiB VM" bir çare DEĞİL: host Mac 18 GiB, VM zaten 14,9 GiB.) Pratik sonuç F4 emsaliyle aynı: lokalde **bileşen bazlı yollar** (Task 1–6 kalıbı), taze-küme **otoriter kapısı CI** |
| 26 | **ArgoCD-only kusur: `Cluster.spec.plugins` SSA atomik + `enabled` CRD default'u → SONSUZ OutOfSync** | CI 35358635069: `application/glue` 15:03'te **Healthy** oldu ama **45 dk boyunca Synced olamadı** ve zaman aşımına uğradı. Teşhis (yeni CI adımları): 3 CNPG Cluster'ı kalıcı `OutOfSync`, controller **13 kez** selfHeal denedi, her seferinde `cluster.postgresql.cnpg.io/… serverside-applied` dedi ve diff kapanmadı. Kök neden CRD şemasından doğrulandı: `spec.plugins` **`x-kubernetes-list-type` TAŞIMIYOR** → SSA onu **atomik** değer görür; öğe içindeki `enabled` alanının **`default: true`**'su var. Biz yazmayınca API sunucusu default'layıp saklıyor (`kubectl get cluster polaris-db -o jsonpath='{.spec.plugins}'` → `[{"enabled":true,"isWALArchiver":true,…}]`), ArgoCD'nin desired'ı ise onsuz → diff asla kapanmaz | `glue/templates/cnpg.yaml` → `plugins: [{name: …, **enabled: true**, isWALArchiver: true, parameters: …}]` (davranış aynı, yalnız desired == live); `backup_test.yaml` bunu kilitliyor. **Bu sınıf hata YALNIZ ArgoCD yolunda görünür** — helm modu diff yapmaz, bu yüzden Task 3/4'ün helm-modu canlı doğrulamaları onu göremezdi; 19. satırdaki PVC/wave bulgusuyla aynı aile. **Genel kural: CRD'de `default`'u olan bir alan, x-kubernetes-list-type'sız bir dizinin içindeyse AÇIKÇA yazılmalıdır.** |

**Taze küme uçtan uca doğrulaması.** **CI (ArgoCD modu, taze runner) YEŞİL: run 35364856165** (`1480aec`, `e2e-kind` işi **50 dk 0 sn**, `helm-unittest` ✅ — yeni "F5 render" adımı dâhil). On işaret sırayla: `E2E F1 OK` (16:08) · `E2E F2 OK` (16:22) · `E2E F3 MONGO OK` (16:28) · `E2E F3 NGINX OK` (16:32) · `E2E F4 TRINO OK` (16:33) · `E2E F4 SUPERSET OK` (16:33) · `E2E F4 JUPYTERHUB OK` (16:35) · `E2E F4 ZEPPELIN OK` (16:35) · **`E2E F5 MONITORING OK`** (16:36) · **`E2E F5 DR OK`** (16:40). Bu koşu aynı zamanda **dev NetworkPolicy AÇIKKEN yapılan ilk tam-yol doğrulamasıdır** (22. satır) ve `E2E_EXPECT_NO_FIRING=1` varsayılanı taze kümede gerçekten tutmuştur (0 ateşlenen Lakehouse alarmı — F4'ten kalan FAILED cron'lar olmadığı için).

CI denemeleri (`helm-unittest` her ikisinde de ✅):

| # | Run | Commit | Sonuç | Kök neden → düzeltme |
|---|---|---|---|---|
| 1 | 35358635069 | `d56fa48` | ❌ | **26. satır**: `application/glue` 45 dk Synced olamadı (CNPG Cluster'ları SSA diff döngüsünde) → `1480aec` |
| 2 | **35364856165** | `1480aec` | ✅ | `e2e-kind` 50 dk 0 sn; on işaret sırayla yeşil |

CI kaynak fotoğrafı (yeşil koşu, `if: always()` adımı): runner `Mem: 15988 MB toplam / 13460 kullanılan / 2528 available`, **swap 3071 MB (232 MB kullanılmış)**, disk `/dev/root 145G %65`; düğüm istekleri `cpu 2760m (%69)` / `memory 8664Mi (%54)`, limitler `cpu 2200m (%55)` / `memory 15510Mi (%97)`. F4'te CPU isteği %65 idi, F5'te %69 — hâlâ rahat. Bellek **limit** taahhüdü %97 görünüyor; bu bir **bilgi**, kapasite alarmı değil: limit toplamı bir tavandır, Kubernetes onu zorlamaz ve tek başına OOM'a yol açmaz (gerçek kullanım: istekler %54, koşu boyunca host OOM yok — 25. satır). Anlamlı sinyal **pod başına gerçek RSS**'tir; ölçülen değerler 25. satırda.

**Lokal (kind/Podman, helm modu) kapanamadı — kayıt için:** üç taze-küme denemesi de 25. satırdaki kontrol düzlemi/operatör **liveness zaman aşımı** kaskadında düştü (sırasıyla `E2E F2 OK` sonrası mongo-path'te, `E2E F1 OK` sonrası pg-path bakımında ve bootstrap'ın glue adımında). Kök neden koşu sonrası canlı ölçümle düzeltildi (25. satır): **host OOM yok, swap farkı ilgisiz, bellek tavanı yok**; PSI'de darboğaz CPU. Denemelerden biri ayrıca **repo dışı** bir ortam tuzağı ortaya çıkardı: `~/.docker/config.json`'daki `credsStore` yüzünden `superset-operator` OCI chart'ı `docker-credential-osxkeychain: executable file not found` ile çekilemiyor → çözüm boş bir `DOCKER_CONFIG` dizini (`runbooks/troubleshooting.md` "Kurulum / ArgoCD"). Lokalde doğrudan kanıtlanan parçalar: F1 (polaris-smoke vended yaz/oku), F2 (3 MERGE + bakım + `rewrite_manifests`), **Grafana dashboard yüklemesi** (24. satır, salt-okunur port-forward) ve Task 1–6'nın bileşen bazlı canlı kanıtlarının tamamı.

Açık kalanlar (F6/pre-ship): **OpenShift UWM** (`enableUserWorkload: true`) ve **OADP** (`DataProtectionApplication` alan adları `kubectl explain` ile doğrulanacak, `velero.namespace: openshift-adp`) canlı — dev'de karşılığı yok; **Alertmanager yönlendirme/bildirim** platformun işi (dev'de `alertmanager: false`, kurallar değerlendirilir ama hiçbir yere gitmez); **Loki canlı** (LokiStack/ClusterLogForwarder, `grafana/alloy` — promtail EOL; repoda kurulum yok, bölüm belge temelli); **PVC içerik yedeği CSI'da** (kind hostPath sınırı, 19. satır — tercih edilen yol CSI snapshot + Data Mover); **tablo düzeyi veri metrikleri** (Prometheus exporter yok → Trino Iceberg metadata tabloları `"tbl$snapshots"`/`"tbl$files"`/`"tbl$history"` + Superset dashboard); **Superset `migrate` DB kapısı** (operator lifecycle Job'u CNPG hazır olmadan başlıyor, `maxRetries: 120` ile örtülüyor); **PDB'ler** (hiçbir bileşende yok); **Kafka MirrorMaker 2** (Kafka verisi DR kapsamı dışında — yeniden akıtma ya da MM2 kararı); **Superset imajı için müşteri aynası + `6.1.0-dev` etiketinin taşınması** (operator `spec.image` digest almıyor); **lokal taze-küme koşusu** (25. satır — denenmemiş çareler: VM'de swap, dev Spark **isteklerinin** daraltılması, `retention: 6h`, probe eşiklerinin gevşetilmesi); **apiserver→webhook NetworkPolicy davranışı OpenShift SDN/OVN'de** (22. satır); ve **F6 cutover** (eski `main`'in emekliye ayrılması, `runbooks/acceptance-tests.md` ile müşteri kabulü).
